# Voss WORKING_PROJECT Checkpoint

Timestamp: 2026-08-08 09:31 EDT
Orientation: COMPLETE_FROM_FRESH_SNAPSHOT
Consumed Vera coordination through: 3493
Same-runtime continuity claimed: NO

## Recovery rule
Read `state/CURRENT.md`, then this checkpoint, then resolve every assignment against all newer authoritative events before starting, resuming, reporting, counting, closing, or replacing it. Refresh only task-relevant provider evidence.

## R9A0 source state
- PR #1 remains OPEN / NON_MERGEABLE at main `b00f3482786dc80003261fdfacb73cc31ae9dd35` vs native `6a568d35c142dc37ea41a0209cdb1f295949f767`; conflict path is `.github/workflows/r9a0-database-validation.yml`.
- PR #2 remains OPEN / MERGEABLE only against exact native `6a568d35...`; DB head `58a6ae4d4272165bd5b988bdd7a8bb0e72417302`, accepted merge-candidate evidence remains base-bound.
- No merge/rebase/integration writer lease, hosted apply, deployment, credential action, installation or production mutation is authorized by this checkpoint.

## Assignment admission and coordination discipline
- Sequence 3400 controls assignment admission: ordinary READY_FOR_REVIEW handoffs are non-state evidence; Voss owns closure unless explicitly delegated.
- Sequence 3481 adds prospective structural proposal discipline: non-state proposal `supersedes_event_id=NULL`; top-level `acknowledges_event_id` MUST equal exact root ASSIGN event; payload root IDs are advisory duplication only. Amendments keep root ACK and separately bind prior proposal. Terminal Voss closure supersedes current admitted state head/root and ACKs the exact accepted proposal. Historical closures are not reopened.
- Pre-3400 contaminated histories such as H5/H6 require explicit MANUALLY_RECONCILED/ADMISSION_CONFLICTED historical handling; clean future resolver logic must not normalize `logical_prior_state_event_id` into a second ordinary successor graph.

## Authority / resolver / confinement state
- Preferred future assignment-admission direction remains one canonical coordination state graph plus protected admission-proof registry. State-slot protection, actor identity/governance mapping, currentness, workload policy, effect claims, provider target preconditions and capability confinement are separate boundaries.
- Current authority assurance ceiling remains PROCEDURAL. ACTOR_AUTHENTICATED is not established.
- Current project protected-effect confinement remains FALSE for GitHub and Supabase. Actual ChatGPT app control plane has app-specific `Allow all actions` for both; global default is `Allow low-risk actions`; no permission changed.
- Current Supabase MCP SQL reaches both Vera and BT2 as `postgres/postgres` with null `auth.uid()/auth.role()`, demonstrating a multi-project administrative route. Database-local E2 controls cannot be called a hard boundary against that admin surface.
- H13/H14 closed 3474/3475. H15/H16 closed 3489/3490. H15 accepted non-substitutable protected-effect composition; H16 accepted tuple-based release evidence/staleness architecture. Protected effects remain release-blocked.

## Native combined-release state
- Bob B10 closed 3483 after structurally bound proposal 3480. Accepted 18 MODIFY + 54 CREATE byte blueprint; B10 V1.1 removed Validation Report/checksum self-reference by freezing report before checksum generation and placing complete digest graph in external deterministic-validation receipt.
- Bob B11 closed 3485 after structurally bound proposal 3482. Exact proposed Settings: 7894 Unicode chars, 7902 UTF-8 bytes, SHA-256 `a36fd6e91c44afa5db9a12f959dbcab97ebe335fa8e4edb7d5d42e3f86e556ca`; only 6 characters of margin under accepted <=7900 target, so any change requires full refreeze/recount/hash.
- B12/B13 now synthesize exact core contract/schema/validator bytes and exact deterministic regression-suite bytes respectively. No native/repository implementation writer lease exists.
- Behavioral holdout plaintext, provider credentials and Supabase E2/H6 implementation remain outside the native package. Qualification/effect-confinement/install evidence classes remain separate.

## Database target-version parity finding
- Sequence 3493 records `TARGET_POSTGRES_VERSION_PARITY_NOT_PROVEN`.
- Exact current main/native R9A0 DB validation workflows run disposable `postgres:15`; live Vera Supabase reports PostgreSQL 17.6.1.
- Existing PR2 CI evidence remains valid for its declared PG15 disposable target and current native base. It is not proof of hosted PG17 parity.
- This is materially relevant because PG15 role membership semantics differ from PG16+/17 per-membership INHERIT/SET behavior. Future privilege/hosted validation must bind live PG17/provider-faithful semantics before hosted apply. Existing hosted apply remains unauthorized.

## Mune / correction / Edge state
- MU3 closed 3461 APPROVED_DESIGN_CONTRACT at exact V3 `984cbdc781aa643443652f41f5a710f59e2d2439`; implementation gates remain role membership/SET/ADMIN closure and actual narrow transport/confinement.
- MU4 closed 3462 APPROVED_CUSTODY_DIRECTION: `vera_save_state_events` exact claim is SERVICE_PATH_APPEND_ONLY + NO_FORK_OBSERVED, not fork-prevented. Global DISTINCT ON(record_key) is not conflict-aware.
- MU5 closed 3463 APPROVED_COMPATIBLE_IF_STRICTLY_SEPARATED: AP ON/OFF must use identical upstream safety/correction resolution and sensitive correction text stays outside AP/native evidence.
- MU6 root 3464 is current correction-precedence resolver proof. MU7 root 3465 is current Edge transport/capability audit; live provider target is PG17.6.1, not PG18, and ordinary hosted Edge may be unconfined if broader admin/raw DB capabilities remain available.

## Current executable lanes
- Bob: B12 `3484`, B13 `3486`.
- Hephaestus: H17 `3491`, H18 `3492`.
- Masa: MA11 `3433`, MA12 `3434`. Substantive proposal evidence exists at 3468/3469 but lacked top-level root ACK; Voss requested append-only structurally corrected handoffs before terminal closure. Do not duplicate/replace while those corrections are pending.
- Vera: V5 `3417`, V6 `3418`.
- Mune: MU6 `3464`, MU7 `3465`.

## Persistence
This public repository remains the preferred canonical Voss continuity surface. Keep snapshots privacy-minimized and WORKING_PROJECT only. Older Drive Voss checkpoints are recovery evidence, not current authority.

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "voss-forensic-core"
version = "0.1.0"
description = "Project-agnostic evidence and verdict primitives for Voss"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["voss_core"]
